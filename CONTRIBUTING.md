# Правила разработки (CONTRIBUTING)

## 🌿 Ветки (Branching Model)

Мы используем упрощенную модель Git Flow.

### Именование веток
Формат: `тип/краткое-описание` или `тип/номер-задачи-описание`

*   `feat/` — новый функционал (например, `feat/login-page`)
*   `fix/` — исправление багов (например, `fix/header-mobile-view`)
*   `refactor/` — рефакторинг кода (например, `refactor/api-client`)
*   `docs/` — обновление документации (например, `docs/git-rules`)
*   `chore/` — рутинные задачи, обновление зависимостей (например, `chore/deps-update`)

---

## 📝 Коммиты (Commit Messages)

Мы следуем стандарту **Conventional Commits**.

### Формат
`<type>(<scope>): <description>`

*   **type**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`.
*   **scope** (опционально): область проекта, которую затронули изменения (например, `ui`, `api`, `auth`).
*   **description**: краткое описание на русском или английском языке.

**Примеры:**
*   `feat(map): добавить инструмент рисования линий`
*   `fix(socket): исправить обрыв соединения при простое`
*   `docs: обновить инструкцию по установке`

---

## 🚀 Процесс работы (Workflow)

1. Создайте ветку от `main` (или `develop`, если есть).
2. Сделайте изменения и закоммитьте их, следуя правилам выше.
3. Отправьте ветку в удаленный репозиторий: `git push origin your-branch`.
4. Создайте Pull Request (PR) в GitHub.
5. После апрува и прохождения тестов, PR вливается в основную ветку.
